extends index

block content
	div 
		em
			a(href="#{link}")= title
	div #{gallery}
	br
	each img in images
		img(class="curated-image" src='/images/' + img)
		br
		br
	div PRESS
	each item in press
		div
			em
				a(href="#{item.url}")= item.text
